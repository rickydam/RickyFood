import { StyleSheet } from "react-native";

const menuItemDetailsStyles = StyleSheet.create({
  label: {
    marginTop: 15,
    textDecorationLine: "underline"
  }
});

module.exports = menuItemDetailsStyles;